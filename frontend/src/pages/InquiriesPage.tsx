export default function InquiriesPage(){
    // GET /inquiries → [{id, facilityName, createdAt, status, ...}]
    return (
        <div className="mx-auto max-w-5xl space-y-3">
            <h2 className="text-xl font-semibold">문의내역</h2>
            {/* 표/카드 + "답변보기" "재문의" 버튼 */}
        </div>
    );
}