server:
    port: ${PORT:8080}
spring:
    application:
        name: CareMate-Backend

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

    jwt:
        secret: ${JWT_SECRET}

ltc:
    api:
        base2: "https://apis.data.go.kr/B550928/getLtcInsttDetailInfoService02"
        serviceKey: ${LTC_SERVICE_KEY}
        rest-api-key: ${KAKAO_REST_API_KEY}

openai:
    api-key: ${OPENAI_API_KEY}
